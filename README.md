# Anchor Timber Site

Production-ready Astro static site for Anchor Timber (Crawford, CO).

## Development
- Install: `npm install`
- Dev server: `npm run dev`
- Build: `npm run build`

## Deployment
GitHub Pages is configured via `.github/workflows/deploy.yml`.
